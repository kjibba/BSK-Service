version: '3.8'

# DEPRECATED: Ikke bruk denne filen.
# Bruk docker-compose.yml for produksjon (Nginx + Node backend + MariaDB).
# Denne filen er beholdt kun for historikk og vil bli fjernet senere.

services:
  _deprecated:
    image: hello-world
    deploy:
      replicas: 0
